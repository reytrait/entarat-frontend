# NestJS Backend Server Environment Variables

# Server Port (default: 3001)
PORT=3001

# Frontend URL for CORS (Next.js app URL)
# For local development: http://localhost:3000
# For production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:3000

# Node Environment
NODE_ENV=development

# Database Configuration (if using external database in future)
# DATABASE_URL=postgresql://user:password@localhost:5432/entarat

# Redis Configuration (if using Redis for sessions/caching in future)
# REDIS_URL=redis://localhost:6379
